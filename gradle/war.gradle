apply plugin: "war"

bootWar {
    mainClassName = "help.logoped.develop.LogopedhelpApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
